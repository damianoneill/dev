version: 2

project_name: dev

before:
  hooks:
    - go mod tidy

builds:
  - id: dev
    main: .
    binary: dev
    ldflags:
      - -s -w
      - -X github.com/damianoneill/dev/cmd.version={{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: dev
    builds: [dev]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: damianoneill
    name: dev
